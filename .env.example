# Supabase (for positional scanner results persistence)
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-or-publishable-key

# Upstox (required for Run Scan / positional scanners — fetches historical candle data)
# Get an access token from Upstox and add it here. Without this, Run Scan will fail.
VITE_UPSTOX_ACCESS_TOKEN=your-upstox-access-token
